extends ../layouts/application.pug

block header
  = t('views.statuses.edit')

block content
  +formFor()(action=route('statuses') method='patch')
    +input(status, 'name', errors)(type="text")
    +submit(status)(value=t('views.statuses.new.save'))
