extends ../layouts/application.pug

block header
  = t('views.session.new.signIn')

block content
  +formFor()(action=route('session') method='post')
    +input(signUpForm, 'email', errors)(type="text")
    +input(signUpForm, 'password')(type="password")
    +submit(signUpForm)(value=t('views.session.new.submit'))
