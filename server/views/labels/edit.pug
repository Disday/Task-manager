extends ../layouts/application.pug

block header
  = t('views.labels.edit')

block content
  +formFor()(action=route('label', {id: label.id} ) method='POST')
    input(name="_method" type="hidden" value="patch")
    +input(label, 'name', errors)(type="text")
    +submit(label)(value=t('views.labels.new.save'))
