extends ../layouts/application.pug

block content
  .card
    .card-body.p-5.bg-light
      .display-4= t('views.welcome.index.hello')
      p.lead= t('views.welcome.index.description')
      hr
      a.btn.btn-success.btn-lg(href=route('newUser'))= t('views.welcome.index.more')
